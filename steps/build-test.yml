parameters:
  SourceDirectory: ''

steps:
  - bash: |
      pip install --upgrade pip wheel setuptools tox pytest
    displayName: 'Install standard packages'
  - bash: |
      python -m pytest
    displayName: 'Run tests'