parameters:
  SourceDirectory: ''

steps:
  - bash: |
      find ~ -name python
    displayName: 'Show python paths'
    workingDirectory: $(System.DefaultWorkingDirectory)
  - bash: |
      echo $PATH
    displayName: 'Show $PATH'
  - bash: |
      python -m tox
    displayName: 'Run tests'
    workingDirectory: $(System.DefaultWorkingDirectory)/${{ parameters.SourceDirectory }}