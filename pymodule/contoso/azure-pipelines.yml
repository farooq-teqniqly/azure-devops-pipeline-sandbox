trigger:
  branches:
    include:
      - 'dev'
      - 'feature/*'
  tags:
    include:
      - '*'
  paths:
    include:
      - 'pymodule/*'
    exclude:
      - '*'

stages:
- template: ../pipelines/archetype-test.yml
  parameters:
    SourceDirectory: ../src